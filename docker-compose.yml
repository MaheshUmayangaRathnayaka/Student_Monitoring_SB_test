version: '3.7'

services:
  server:
    image: maheshur/student-performance-server:latest
    container_name: myapp-node-server
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - ./server/.env
    environment:
      - NODE_ENV=production
      - CLIENT_URL=http://43.205.210.137:3000
    networks:
      - app-network

  client:
    image: maheshur/student-performance-client:latest
    container_name: myapp-react-client
    restart: always
    depends_on:
      - server
    ports:
      - "3000:80"
    environment:
      - VITE_API_URL=http://43.205.210.137:5000/api
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
